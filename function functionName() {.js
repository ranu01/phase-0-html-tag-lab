
/*function greetings(firstName, timeOfDay){
    console.log(`Good ${timeOfDay} ${firstName},Have a nice day`);
    
    }
    
    function greetJane(){
        console.log('Good Morning, Jane have a nice day')
     }
    
    
    greetings('tony', 'eveving');
    greetings('john','afternoon');
    greetings('lewis','evening');
    greetings('jack', 'goodnight');

    const age = 5;

let isAdult, canWork, canEnlist, canDrink;

switch (true) {
    case age >= 21:
        canDrink = true;
    case age >= 18:
        isAdult = true;
        canEnlist = true;
    case age >= 16:
        canWork = true;
}
// => true

isAdult;
// => true

canWork;
// => true

canEnlist;
// => true

canDrink;

//function name(str) {
   // console.log(str(0).toUpperCase());
   // console.log(str.charAt(0))
    //console.log(str.slice(3))
   // return str.charAt(0).toUpperCase() + str.slice(1);
///}
 //n*/


 function calculate(numberOne, numberTwo){
    const sumOfTwoNumbers=numberOne + numberTwo
    console.log(sumOfTwoNumbers)
    return sumOfTwoNumbers,
 }7
 
const another = calculate(3, 5)
    console.log(another)